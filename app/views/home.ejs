<!DOCTYPE html>
<html>

<head>
  <title>User Data Fetch</title>
</head>

<body>
  <h1>Welcome to Your Home Page</h1>
  <button id="loginButton">Login</button>
  <button id="fetchUserDataButton">Fetch User Data</button>
  <div id="userDataDisplay"></div>
  <script src="script.js"></script>
</body>

</html>


<script>
  document.addEventListener("DOMContentLoaded", function () {
    const loginButton = document.getElementById("loginButton");
    const fetchUserDataButton = document.getElementById("fetchUserDataButton");
    const userDataDisplay = document.getElementById("userDataDisplay");

    // Replace 'your_api_endpoint' with the actual API endpoint to retrieve user data.

    fetchUserDataButton.addEventListener("click", function () {
      // Replace 'your_token' with the user's authentication token.
      const userToken = 'your_token';

      // Make a GET request to the API endpoint with the token.
      fetch('/user/login', {
        Authorization: token,
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ email, password }),
      })
        .then(response => {
          if (response.status === 200) {
            return response.json();
          } else {
            throw new Error("Unable to fetch user data.");
          }
        })
        .then(data => {
          // Display the user data on the page.
          userDataDisplay.innerHTML = `<p>Name: ${data.name}</p><p>Email: ${data.email}</p>`;
        })
        .catch(error => {
          console.error(error);
          userDataDisplay.innerHTML = "Error fetching user data.";
        });
    });
  });
</script>